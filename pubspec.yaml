name: app_dev
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  cloud_firestore: ^4.15.8
  collection: ^1.18.0
  firebase_auth: ^4.17.8
  firebase_core: ^2.27.0
  flutter:
    sdk: flutter
  flutter_credit_card: ^4.0.1
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  provider: ^6.1.2
  flutter_riverpod: ^2.5.1

flutter:
  uses-material-design: true

  assets:
    - assets/images/pistols/
    - assets/images/rifles/
    - assets/images/shotguns/
    - assets/images/smg/
    - assets/images/snipers/

